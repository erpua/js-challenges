const newString = 'javascriptloops';

function vowelsAndConsonants(newString) {
    const newStringLength = newString.length;
    
    for (let i = 0; i < newStringLength; i++) {
        if (([i] == "a") || ([i] == "e") || ([i] == "i") || ([i] == "o") || (s[i] == "u")) {
              console.log([i]); //prints all vowels
        }
    }

    //TODO


};

vowelsAndConsonants(newString);
